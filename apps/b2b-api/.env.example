# B2B API Environment Configuration

# Server
NODE_ENV=development
PORT=3001

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/proxify

# Logging
LOG_LEVEL=info

# Privy Credentials
# Get these from https://dashboard.privy.io
PRIVY_APP_ID=
PRIVY_APP_SECRET=

# Blockchain Network Configuration
# ================================
# Network selection is based on NODE_ENV:
#   - development/test → Sepolia Testnet (chainId: 11155111)
#   - production → Ethereum Mainnet (chainId: 1)

# Ethereum Mainnet RPC URL (required for production)
MAINNET_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY

# Sepolia Testnet RPC URL (required for development)
SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_KEY

# Legacy: CHAIN_ID (deprecated, auto-detected from NODE_ENV)
# Kept for backward compatibility. Will be removed in future versions.
# CHAIN_ID=11155111

# NOTE: Token addresses (USDC, USDT, WETH, etc.) are now defined in:
# @quirk/core/constants/networks.ts (NETWORK_CONFIG)
# Access via: NETWORK_CONFIG.eth_sepolia.token.usdc.address
# No need to set token addresses as environment variables.

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# ORACLE PRIVATE KEYS (for token operations)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# These are the hot wallet private keys used to fund custodial wallets
# IMPORTANT: Keep these secure! Use a secrets manager in production.

# SANDBOX: Private key for MockUSDC contract owner (minting on Sepolia)
# Used when environment='sandbox' - mints MockUSDC tokens to custodial wallet
# Get this from: Your Hardhat deployment or contract owner account
SANDBOX_ORACLE_PRIVATE_KEY=0x_your_sandbox_private_key_here
SEPOLIA_USDC_TOKEN_ADDRESS=0x2DA55f4c1eCEB0cEeB93ee598e852Bf24Abb8FcE

# PRODUCTION: Private key for oracle wallet holding real USDC (transfers on Mainnet)
# Used when environment='production' - transfers real USDC to custodial wallet
# CRITICAL: This wallet must be funded with real USDC before production use!
MAINNET_ORACLE_PRIVATE_KEY=0x_your_mainnet_private_key_here

# Legacy aliases (kept for backwards compatibility)
DEPLOYER_PRIVATE_KEY=your_private_key_here

